import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="YXNkZmFzZGZhc2Q"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="ZG9uJ2ZvcmdlZC4"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="cGFzc3dvcmQhIQ"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="bWVtYmVyc2hpcC4"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="aXNzdWVwYXNzLg"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="dHJhbnNwb3J0Lg"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="ZGVsZXRlZCBjbw"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="Y2FzdGluZyBkbg"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="bGFuZHNjYXBlLg"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="aHR0cHM6Ly9naX"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="dW5kZWZpbmVkLg"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="ZGVsZXRpbmcgcw"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="Zm9yY2UgYm94Lg"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="Y29tcGxleCBmLg"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="ZW5jb2RlZCBzbg"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="aGVGaW5hbFNwZWxs"

 

    print(f'''

    {userName} (@{userLogin}), you are about to initiate the {spell}. Are you sure it?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="YXV0b21hdGUgZg"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="c29tZSBkYXRhIGM"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="Uh1bnRzbWFuNC9U"
    print(f'''
    {userName} (@{userLogin}), you have found the right spell. As a part of your reward for solving the riddle,
    here is the secret code assosciated with it: {code}
    ''')

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="ZGVsZXRlciBzbg"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="c3RpbGwgY29tcA"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")import subprocess
import json
import os
try:
    userData = subprocess.getstatusoutput(f'gh api -H "Accept: application/vnd.github+json" /user')
    user=json.loads(userData[1])
    userName=user['name']
    userLogin=user['login']
    file=os.path.basename(__file__)
    spell=file.split('.')[0]
    code="RodWIuY29tL1RoZ"

 

    print(f'''

    {userName} (@{userLogin}), you are about to use the {spell} spell. Are you sure this is the right one?
    If yes, then the secret code assosciated with it is:
    {code}
        ''')
       

except Exception as e:
    print("Unkown Error occured, please try again later")